@{
    ViewData["Title"] = "Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">
<head>
    <meta charset="utf-8" />
    <title>Login - MiAula</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #121212, #1e1e1e);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-card {
            background: #1c1c1c;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 0 25px rgba(0, 196, 255, 0.3);
            width: 100%;
            max-width: 400px;
            animation: fadeInUp 0.8s ease;
        }

            .login-card h2 {
                color: #00c4ff;
                text-align: center;
                margin-bottom: 1rem;
            }

        .form-control {
            background: #2a2a2a;
            border: none;
            color: #fff;
            border-radius: 10px;
            transition: 0.3s;
        }

            .form-control:focus {
                background: #333;
                box-shadow: 0 0 10px #00c4ff;
                color: #fff;
            }

        .btn-login {
            background: linear-gradient(90deg, #00c4ff, #1de9b6);
            border: none;
            color: #000;
            font-weight: bold;
            border-radius: 10px;
            transition: 0.3s;
        }

            .btn-login:hover {
                box-shadow: 0 0 15px #00c4ff;
                transform: translateY(-2px);
            }

        .extra-links {
            text-align: center;
            margin-top: 1rem;
        }

            .extra-links a {
                color: #00c4ff;
                text-decoration: none;
                transition: 0.3s;
            }

                .extra-links a:hover {
                    color: #1de9b6;
                }

        /* Animación entrada */
        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Animación para el ícono educativo */
        .edu-icon {
            font-size: 3rem;
            color: #1de9b6;
            display: block;
            margin: 0 auto 1rem;
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0%, 100% {
                transform: scale(1);
                color: #1de9b6;
            }

            50% {
                transform: scale(1.1);
                color: #00c4ff;
            }
        }
    </style>
</head>
<body>
    <div class="login-card">
        <i class="fas fa-graduation-cap edu-icon"></i>
        <h2>Bienvenido a MiAula</h2>
        <form method="post" asp-controller="Account" asp-action="Login">
            <div class="mb-3">
                <label class="form-label"><i class="fas fa-user"></i> Usuario</label>
                <input type="text" class="form-control" name="Username" placeholder="Ingresa tu usuario" required>
            </div>
            <div class="mb-3">
                <label class="form-label"><i class="fas fa-lock"></i> Contraseña</label>
                <input type="password" class="form-control" name="Password" placeholder="Ingresa tu contraseña" required>
            </div>
            <button type="submit" class="btn btn-login w-100">
                <i class="fas fa-sign-in-alt"></i> Ingresar
            </button>
        </form>
        <div class="extra-links">
            <a href="#"><i class="fas fa-key"></i> ¿Olvidaste tu contraseña?</a><br>
            <a href="#"><i class="fas fa-user-plus"></i> Crear cuenta nueva</a>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
