<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notas de Clase - Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            backdrop-filter: blur(10px);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 3px solid #667eea;
        }

            .header h1 {
                color: #333;
                font-size: 2.5rem;
                margin-bottom: 10px;
                text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
            }

        .class-info {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .info-item {
            text-align: center;
        }

        .info-label {
            font-size: 0.9rem;
            opacity: 0.8;
            margin-bottom: 5px;
        }

        .info-value {
            font-size: 1.4rem;
            font-weight: bold;
        }

        .students-grid {
            display: grid;
            gap: 25px;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        }

        .student-card {
            background: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            border-left: 5px solid #667eea;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

            .student-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 15px 35px rgba(0,0,0,0.15);
            }

            .student-card::before {
                content: '';
                position: absolute;
                top: 0;
                right: 0;
                width: 60px;
                height: 60px;
                background: linear-gradient(45deg, #667eea, #764ba2);
                border-radius: 0 0 0 100%;
                opacity: 0.1;
            }

        .student-name {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .student-id {
            background: #667eea;
            color: white;
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: normal;
        }

        .grades-section {
            margin-bottom: 20px;
        }

        .section-title {
            color: #555;
            font-weight: 600;
            margin-bottom: 15px;
            font-size: 1.1rem;
            border-bottom: 2px solid #eee;
            padding-bottom: 5px;
        }

        .tasks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); /* Aumentado a 100px */
            gap: 15px; /* Aumentado a 15px */
            margin-bottom: 15px;
        }

        .task-grade {
            background: #f8f9fa;
            padding: 15px; /* Aumentado a 15px */
            border-radius: 8px;
            text-align: center;
            border: 2px solid transparent;
            transition: all 0.2s ease;
        }

            .task-grade:hover {
                background: #e9ecef;
                border-color: #667eea;
            }

        .task-number {
            font-size: 1rem; /* Aumentado a 1rem */
            color: #666;
            margin-bottom: 8px; /* Aumentado a 8px */
        }

        .task-score {
            font-weight: bold;
            font-size: 1.4rem; /* Aumentado a 1.4rem */
            color: #333;
        }

        .exam-grade {
            background: linear-gradient(45deg, #ff6b6b, #ee5a52);
            color: white;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 15px;
        }

        .exam-label {
            font-size: 0.9rem;
            opacity: 0.9;
            margin-bottom: 5px;
        }

        .exam-score {
            font-size: 1.8rem;
            font-weight: bold;
        }

        .final-grade {
            background: linear-gradient(45deg, #51cf66, #40c057);
            color: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

            .final-grade::before {
                content: '🏆';
                position: absolute;
                top: 10px;
                right: 15px;
                font-size: 2rem;
                opacity: 0.3;
            }

        .final-label {
            font-size: 1rem;
            opacity: 0.9;
            margin-bottom: 5px;
        }

        .final-score {
            font-size: 2.2rem;
            font-weight: bold;
        }

        .grade-excellent {
            color: #51cf66;
        }

        .grade-good {
            color: #339af0;
        }

        .grade-average {
            color: #ffd43b;
        }

        .grade-poor {
            color: #ff6b6b;
        }

        .stats-summary {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
        }

        .stat-item {
            text-align: center;
            padding: 10px;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: #667eea;
            display: block;
        }

        .stat-label {
            color: #666;
            font-size: 0.9rem;
            margin-top: 5px;
        }

        header, footer {
            text-align: center;
            padding: 20px;
            color: #fff;
            background: rgba(0, 0, 0, 0.2);
        }

        header {
            margin-bottom: 20px;
            border-radius: 15px 15px 0 0;
            font-size: 1.5rem;
            font-weight: bold;
        }

        footer {
            margin-top: 20px;
            border-radius: 0 0 15px 15px;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <header>MiAula</header>
    <div class="container">
        <div class="header">
            <h1>📚 Dashboard de Notas</h1>
            <p>Sistema de Gestión de Calificaciones</p>
        </div>

        <div class="class-info">
            <div class="info-item">
                <div class="info-label">Clase</div>
                <div class="info-value">Matemáticas Avanzadas</div>
            </div>
            <div class="info-item">
                <div class="info-label">Periodo</div>
                <div class="info-value">2024-1</div>
            </div>
        </div>

        <div class="students-grid">
            <div class="student-card">
                <div class="student-name">
                    @Model[0].alumno.Nombre @Model[0].alumno.Apellido
                    <span class="student-id">ID: @Model[0].alumno.Id</span>
                </div>

                <div class="grades-section">
                    <div class="section-title">📝 Tareas (@Model[0].planificacion.Porcentaje_Tareas%)</div>
                    <div class="tasks-grid">
                        @foreach (var p in Model)
                        {
                            <div class="task-grade">
                                <div class="task-number">Tarea @p.tarea.Id_Tarea</div>
                                <div class="task-score">@p.nota</div>
                            </div>
                        }
                    </div>
                </div>

                <div class="exam-grade">
                    <div class="exam-label">📋 Examen Final (@Model[0].planificacion.Porcentaje_Examenes%)</div>
                    <div class="exam-score">89</div>
                </div>

                <div class="final-grade">
                    <div class="final-label">🎯 Nota Final</div>
                    <div class="final-score">90.4</div>
                </div>
            </div>
        </div>
    </div>
    <footer>© 2025 - MiAula. Todos los derechos reservados.</footer>
</body>
</html>